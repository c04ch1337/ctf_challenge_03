#!/bin/bash
# Hide flags in the web directory
echo "Setting up flags..."
echo "CTF{flag3_sql_injection}" > /var/www/html/flag3.txt
echo "CTF{flag4_directory_traversal}" > /var/www/html/flag4.txt
# Add more flags as needed
